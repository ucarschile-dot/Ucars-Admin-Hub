import { StitchScreenFrame } from '@/components/stitch-screen-frame';

export default function LeadsPage() {
  // Ruta del tablero de prospectos dentro del contenedor de pantallas.
  return <StitchScreenFrame screen="leads" />;
}
